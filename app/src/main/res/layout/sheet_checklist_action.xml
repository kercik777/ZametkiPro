<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="10dp"
    android:paddingBottom="14dp">

    <View
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="10dp"
        android:background="@drawable/bg_sheet_handle" />

    <TextView
        style="@style/SheetSubtitle"
        android:text="@string/settings_checklist_group_desc" />

    <RadioGroup
        android:id="@+id/rg_checklist_action"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingTop="4dp"
        android:paddingBottom="4dp">

        <RadioButton
            android:id="@+id/rb_move_to_bottom"
            style="@style/SettingsRadioRow"
            android:text="@string/checklist_action_move_to_bottom" />

        <RadioButton
            android:id="@+id/rb_leave_place"
            style="@style/SettingsRadioRow"
            android:text="@string/checklist_action_leave_place" />

        <RadioButton
            android:id="@+id/rb_delete"
            style="@style/SettingsRadioRow"
            android:text="@string/checklist_action_delete" />
    </RadioGroup>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end"
        android:layout_marginTop="6dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        tools:ignore="UseCompoundDrawables">

        <TextView
            android:id="@+id/btn_cancel"
            android:layout_width="wrap_content"
            android:layout_height="44dp"
            android:gravity="center"
            android:paddingStart="18dp"
            android:paddingEnd="18dp"
            android:textColor="@color/text_secondary"
            android:textSize="14sp"
            android:text="@string/cancel"
            android:background="@drawable/bg_ripple_rounded" />
    </LinearLayout>
</LinearLayout>
